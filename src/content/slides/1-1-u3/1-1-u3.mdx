import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { StepCard } from '@/components/cards/step-card'
import { NoteCard } from '@/components/cards/note-card'
import { SummaryCard } from '@/components/cards/summary-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { PrimeDivisionLadderMotion } from '@/components/content/prime-division-ladder-motion'
import { PrimeDivisionStep2Activity } from '@/components/content/prime-division-step2-activity'
import { FactorTreeStep3Activity } from '@/components/content/factor-tree-step3-activity'
import { PrimeFactorTreeMotion } from '@/components/content/prime-factor-tree-motion'
import { PrimeFactorizationTermMotion } from '@/components/content/prime-factorization-term-motion'
import { QuizArea } from '@/components/quiz'
import { MathInline } from '@/components/math'
import { steps } from './steps-data'

export const meta = {
  slug: '1-1-u3-prime-factorization',
  title: '소인수분해',
  subtitle: '중1 수학 · 소인수분해',
}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg="/images/shape_2.png"
    speechText="자연수를 소수들의 곱으로 바꾸는 핵심 기술, 소인수분해를 시작해 보자!"
    topicTitle="03. 소인수분해"
  >
    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>자연수를 소수의 곱으로 나타내는 것을 {blank}라고 한다.</>
        )}
      />
    )}
  </IntroCard>
</Step>

<Step id={1}>
  <PrimeFactorizationTermMotion>
    {steps[1].quiz && (
      <QuizArea
        stepId={1}
        quiz={steps[1].quiz}
        renderBlank={(blank) => (
          <>12의 소인수분해꼴은 {blank}</>
        )}
      />
    )}
  </PrimeFactorizationTermMotion>
</Step>

<Step id={2}>
  <StepCard variant="white">
    <div className="mb-2 text-center text-base font-extrabold text-slide-gray">
      <span className="rounded-md bg-[#2d67a8] px-2 py-1 text-xs font-extrabold text-white">소인수분해 방법1</span>(소수로 나누기)
    </div>
    <PrimeDivisionStep2Activity stepId={2} />
  </StepCard>
</Step>

<Step id={3}>
  <StepCard variant="white">
    <div className="mb-2 text-center text-base font-extrabold text-slide-gray">
      <span className="rounded-md bg-[#2d67a8] px-2 py-1 text-xs font-extrabold text-white">소인수분해 방법2</span>(두 수의 곱으로 가르기)
    </div>
    <FactorTreeStep3Activity stepId={3} />
  </StepCard>
</Step>

<Step id={4}>
  <StepCard variant="white">
    <div className="text-center text-[15px] font-bold text-slide-gray leading-[1.9] mb-3">
      소인수분해한 결과는 반드시<br /><span className="font-extrabold text-[#2d67a8]">소수들만의 곱</span>으로 써야 한다.
    </div>

    <div className="mx-auto mb-3 rounded-lg bg-[#f5f5f5] px-2 py-2">
      <div className="flex items-start gap-1">
        <span className="shrink-0 rounded bg-[#2d67a8] px-1.5 py-0.5 text-[11px] font-bold text-white">예</span>
        <div className="text-[13px] font-bold text-slide-gray leading-[2]">
          <MathInline tex={"60 = 2 \\times 2 \\times 3 \\times 5 = 2^2 \\times 15"} />
          <span className="ml-1 font-extrabold text-red-500">(✕)</span>
          <br />
          <span className="ml-5"><MathInline tex={"="} /></span> <MathInline tex={"2^2 \\times 3 \\times 5"} />
          <span className="ml-1 font-extrabold text-blue-600">(○)</span>
        </div>
      </div>
    </div>

    <div className="text-center text-[15px] font-bold text-slide-gray leading-[1.9] mb-3">
      소인수분해한 결과에서 밑이 <span className="rounded bg-yellow-200 px-1 font-extrabold text-[#2d67a8]">소인수</span> 이다.
    </div>

    <div className="mx-auto rounded-lg bg-[#f5f5f5] px-2 py-2 mb-2">
      <div className="flex items-start gap-1">
        <span className="shrink-0 rounded bg-[#2d67a8] px-1.5 py-0.5 text-[11px] font-bold text-white">예</span>
        <div className="text-[15px] font-bold text-slide-gray leading-[2]">
          <MathInline tex={"60 = {\\color{red}2}^2 \\times {\\color{red}3} \\times {\\color{red}5}"} /> 이므로 60의 소인수는 <span className="font-extrabold text-red-500">2, 3, 5</span>이다.
        </div>
      </div>
    </div>

    {steps[4].quiz && (
      <QuizArea
        stepId={4}
        quiz={steps[4].quiz}
        renderBlank={(blank) => (
          <>60의 소인수의 개수는 {blank}개이다.</>
        )}
      />
    )}
  </StepCard>
</Step>

<Step id={5}>
  <SummaryCard
    title="소인수분해 핵심 정리"
    cases={[
      { tag: '목표', tagVariant: 'equal', content: <>자연수를 소수들의 곱으로 나타낸다.</> },
      { tag: '방법 1', tagVariant: 'big', content: <>작은 소수부터 차례로 나눈다.</> },
      { tag: '방법 2', tagVariant: 'small', content: <>두 수의 곱으로 계속 가른다.</> },
    ]}
  >
    {steps[5].quiz && (
      <QuizArea
        stepId={5}
        quiz={steps[5].quiz}
        renderBlank={(blank) => (
          <>소인수분해는 {blank}으로 <br />나타나야 한다.</>
        )}
      />
    )}
  </SummaryCard>
</Step>

<Step id={6}>
  <CompleteCard emoji="🎉" title="소인수분해 완료!">
    소수로 나누기와 두 수의 곱으로 가르기,
    <br />
    두 가지 방법으로 소인수분해를 익혔어요.
    <br />
    결과는 반드시 소수들만의 곱! 잊지 마세요.
  </CompleteCard>
</Step>

</SlideContent>
