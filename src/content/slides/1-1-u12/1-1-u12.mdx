import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { StepCard } from '@/components/cards/step-card'
import { QuizArea } from '@/components/quiz'
import {
  SameSignAdditionExpressionStage,
  SameSignAdditionFormulaStage,
  SameSignAdditionNumberLineStage,
} from '@/components/content/same-sign-addition-stages'
import { SameSignAdditionPracticeCarousel } from '@/components/content/same-sign-addition-practice-carousel'
import { steps } from './steps-data'
import shape2Character from '@/assets/images/shape_2.png'

export const meta = {
  slug: '1-1-u12-addition',
  title: '덧셈',
  subtitle: '중1 수학 · 정수와 유리수',
}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg={shape2Character}
    speechText={`이번 시간에 배울 내용
1. 같은 부호의 두 수를 식으로 더하기
2. 다른 부호의 두 수를 식과 수직선으로 더하기
3. 부호와 절댓값으로 덧셈 정리하기`}
    topicTitle="12. 덧셈"
  >
    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <div className="mx-auto max-w-[430px]">
            <div className="rounded-[18px] bg-white px-5 py-4 text-center shadow-sm">
              <div className="mb-3 text-[15px] font-extrabold text-[#2E7D57]">
                같은 부호끼리 더하면?
              </div>
              <div className="flex flex-wrap items-center justify-center gap-x-2 gap-y-1 text-[30px] font-black leading-[1.4] text-[#1F2937]">
                <span className="whitespace-nowrap">(+2)+(+1)</span>
                <span>=</span>
                <span className="inline-flex items-center whitespace-nowrap align-middle [&>button]:min-w-[44px] [&>button]:px-2 [&>button>span]:text-[17px]">
                  {blank}3
                </span>
              </div>
            </div>
          </div>
        )}
      />
    )}
  </IntroCard>
</Step>

<Step id={1}>
  <SameSignAdditionExpressionStage stepId={1} sign="positive" quiz={steps[1].quiz} />
</Step>

<Step id={2}>
  <SameSignAdditionExpressionStage stepId={2} sign="negative" quiz={steps[2].quiz} />
</Step>

<Step id={3}>
  <SameSignAdditionNumberLineStage stepId={3} sign="positive" quiz={steps[3].quiz} />
</Step>

<Step id={4}>
  <SameSignAdditionNumberLineStage
    stepId={4}
    sign="negative"
    quizTarget="total"
    quiz={steps[4].quiz}
  />
</Step>

<Step id={5}>
  <SameSignAdditionFormulaStage stepId={5} sign="positive" quiz={steps[5].quiz} />
</Step>

<Step id={6}>
  <SameSignAdditionFormulaStage stepId={6} sign="negative" quiz={steps[6].quiz} />
</Step>

<Step id={7}>
  <PracticeCard title="같은 부호의 덧셈 확인문제" subtitle="공통의 부호를 쓰고 절댓값끼리 더하자">
    {steps[7].quiz && (
      <QuizArea
        stepId={7}
        quiz={steps[7].quiz}
        renderBlanks={(blank, solvedAnswers, meta) => (
          <SameSignAdditionPracticeCarousel
            blank={blank}
            solvedAnswers={solvedAnswers}
            recentCorrectBlankId={meta.recentCorrectBlankId}
          />
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={8}>
  <h2 className="mb-6 border-b-2 border-[#2E7D57] py-4 text-center text-[28px] font-black text-[#2E7D57]">다른 부호의 덧셈</h2>
  <SameSignAdditionExpressionStage stepId={8} sign="positive" differentSigns quiz={steps[8].quiz} />
</Step>

<Step id={9}>
  <SameSignAdditionExpressionStage stepId={9} sign="negative" differentSigns quiz={steps[9].quiz} />
</Step>

<Step id={10}>
  <SameSignAdditionNumberLineStage stepId={10} sign="positive" differentSigns quizTarget="total" quiz={steps[10].quiz} />
</Step>

<Step id={11}>
  <SameSignAdditionNumberLineStage stepId={11} sign="negative" differentSigns quizTarget="total" quiz={steps[11].quiz} />
</Step>

<Step id={12}>
  <SameSignAdditionFormulaStage stepId={12} sign="positive" differentSigns quiz={steps[12].quiz} />
</Step>

<Step id={13}>
  <SameSignAdditionFormulaStage stepId={13} sign="negative" differentSigns quiz={steps[13].quiz} />
</Step>

<Step id={14}>
  <StepCard variant="white">
    <h2 className="text-center text-[24px] font-black text-[#2E7D57]">절댓값이 같을 때</h2>
    <p className="mt-4 text-center text-[18px] leading-relaxed">부호가 다르고 절댓값이 같으면 합은 0이에요.</p>
    <QuizArea stepId={14} quiz={steps[14].quiz} renderBlank={(blank) => (
      <div className="flex flex-wrap items-center justify-center gap-2 text-[26px] font-black">
        <span className="whitespace-nowrap">(+3)+(−3)</span><span>=</span>{blank}
      </div>
    )} />
  </StepCard>
</Step>

<Step id={15}>
  <PracticeCard title="다른 부호의 덧셈 확인문제" subtitle="절댓값이 큰 수의 부호를 쓰고 절댓값의 차를 구하자">
    <QuizArea stepId={15} quiz={steps[15].quiz} renderBlanks={(blank) => (
      <div className="grid gap-6 text-center text-[24px] font-black">
        <div className="flex flex-wrap items-center justify-center gap-2"><span>(−3)+(+7)=</span>{blank('positive')}</div>
        <div className="flex flex-wrap items-center justify-center gap-2"><span>(+3)+(−7)=</span>{blank('negative')}</div>
        <div className="flex flex-wrap items-center justify-center gap-2"><span>(−5)+(+5)=</span>{blank('zero')}</div>
      </div>
    )} />
  </PracticeCard>
</Step>

<Step id={16}>
  <CompleteCard emoji="➕" title="같은 부호와 다른 부호의 덧셈 완료!">
    같은 부호: 공통의 부호와 절댓값의 합
    <br />
    다른 부호: 절댓값이 큰 수의 부호와 절댓값의 차
    <br />
    부호가 다르고 절댓값이 같으면 합은 0이에요.
  </CompleteCard>
</Step>

</SlideContent>
