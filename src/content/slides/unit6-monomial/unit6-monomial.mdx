{/* ── Imports ── */}
import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { ConceptCard } from '@/components/cards/concept-card'
import { LawCard } from '@/components/cards/law-card'
import { NoteCard, NoteExample } from '@/components/cards/note-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { QuizArea } from '@/components/quiz'
import { MathInline } from '@/components/math'
import { LawExample } from '@/components/content/law-example'
import { SolutionSteps, SolutionLine, SolutionArrow } from '@/components/content/solution-steps'
import { steps } from './steps-data'

export const meta = {
  slug: 'unit6-monomial',
  title: '단항식의 곱셈과 나눗셈',
  subtitle: '중2 수학 · 단원2',
}

{/* ──────────────────────────────────────────────── */}
{/* Step 0 — 인트로                                  */}
{/* ──────────────────────────────────────────────── */}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg="/images/shape_2.png"
    speechText="단항식의 곱셈과 나눗셈을 배워보자~"
    topicTitle="단항식의 곱셈과 나눗셈"
  >
    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>수와 문자의 곱으로만 이루어진 식을 {blank} (이)라 해요</>
        )}
      />
    )}
  </IntroCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 1 — 곱셈의 교환법칙                          */}
{/* ──────────────────────────────────────────────── */}

<Step id={1}>
  <LawCard
    title="곱셈의 교환법칙"
    formula={"\\color{#F0473D}{x} \\times \\color{#00AAEE}{y} = \\color{#00AAEE}{y} \\times \\color{#F0473D}{x}"}
    hint="💡 순서를 바꿔서 곱해도 OK!"
  >
    {steps[1].quiz && (
      <QuizArea
        stepId={1}
        quiz={steps[1].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\color{#F0473D}{x} \\times \\color{#00AAEE}{y} ="} /> {blank}
          </>
        )}
      />
    )}
  </LawCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 2 — 곱셈의 결합법칙                          */}
{/* ──────────────────────────────────────────────── */}

<Step id={2}>
  <LawCard
    title="곱셈의 결합법칙"
    formula={"\\color{#F0473D}{x} \\times (\\color{#00AAEE}{y} \\times \\color{#059373}{z}) = (\\color{#F0473D}{x} \\times \\color{#00AAEE}{y}) \\times \\color{#059373}{z}"}
    hint="💡 세 개 이상의 곱에서 어느 것을 먼저 곱해도 OK!"
  >
    {steps[2].quiz && (
      <QuizArea
        stepId={2}
        quiz={steps[2].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"x \\times (y \\times z) = (x \\times y) \\times"} /> {blank}
          </>
        )}
      />
    )}
  </LawCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 3 — (단항식) × (단항식) 개념                 */}
{/* ──────────────────────────────────────────────── */}

<Step id={3}>
  <ConceptCard
    variant="white"
    formula={"(\\text{단항식}) \\times (\\text{단항식})"}
    formulaSize="katex-formula"
    description="수는 수끼리, 문자는 문자끼리 곱하기!"
  >
    <div className="mt-3 space-y-2 text-sm">
      <div className="p-2.5 px-3.5 bg-slide-warning-bg rounded-[10px] text-center font-bold text-slide-brown">
        📝 답을 쓸 때: 수는 문자보다 앞에! 문자는 알파벳 순서대로!
      </div>
    </div>
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 4 — -5a² × 4ab 풀이 과정                    */}
{/* ──────────────────────────────────────────────── */}

<Step id={4}>
  <ConceptCard
    variant="default"
    formula={"\\color{#059373}{-5}\\color{#EE3898}{a^2} \\times \\color{#059373}{4}\\color{#EE3898}{a}\\color{#EE3898}{b}"}
    formulaSize="katex-law"
  >
    <SolutionSteps>
      <SolutionLine label="생략된 곱셈 기호 쓰기">
        <MathInline tex={"-5 \\times a^2 \\times 4 \\times a \\times b"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine label="수끼리! 문자끼리! 모으기">
        <MathInline tex={"-5 \\times 4 \\times a^2 \\times a \\times b"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine>
        <MathInline tex={"= -20a^3b"} />
      </SolutionLine>
    </SolutionSteps>
    {steps[4].quiz && (
      <QuizArea
        stepId={4}
        quiz={steps[4].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"-5a^2 \\times 4ab ="} /> {blank}
          </>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 5 — 연습 (1) 4x × (-5y)                    */}
{/* ──────────────────────────────────────────────── */}

<Step id={5}>
  <PracticeCard title="📝 연습 (1)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"4x \\times (-5y)"} />
    </div>
    {steps[5].quiz && (
      <QuizArea
        stepId={5}
        quiz={steps[5].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"4x \\times (-5y) ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 6 — 연습 (2) 2a³ × 7a                      */}
{/* ──────────────────────────────────────────────── */}

<Step id={6}>
  <PracticeCard title="📝 연습 (2)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"2a^3 \\times 7a"} />
    </div>
    {steps[6].quiz && (
      <QuizArea
        stepId={6}
        quiz={steps[6].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"2a^3 \\times 7a = 14a"} />{blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 7 — 연습 (3) 3ab × 10b                     */}
{/* ──────────────────────────────────────────────── */}

<Step id={7}>
  <PracticeCard title="📝 연습 (3)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"3ab \\times 10b"} />
    </div>
    {steps[7].quiz && (
      <QuizArea
        stepId={7}
        quiz={steps[7].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"3ab \\times 10b ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 8 — 연습 (4) ½a × (-16b)                   */}
{/* ──────────────────────────────────────────────── */}

<Step id={8}>
  <PracticeCard title="📝 연습 (4)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"\\frac{1}{2}a \\times (-16b)"} />
    </div>
    {steps[8].quiz && (
      <QuizArea
        stepId={8}
        quiz={steps[8].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{1}{2}a \\times (-16b) ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 9 — 연습 (5) -5x² × (-⅔)x                 */}
{/* ──────────────────────────────────────────────── */}

<Step id={9}>
  <PracticeCard title="📝 연습 (5)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"-5x^2 \\times \\left(-\\frac{2}{3}\\right)x"} />
    </div>
    {steps[9].quiz && (
      <QuizArea
        stepId={9}
        quiz={steps[9].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"-5x^2 \\times \\left(-\\frac{2}{3}\\right)x ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 10 — 나눗셈 → 분수의 곱셈                    */}
{/* ──────────────────────────────────────────────── */}

<Step id={10}>
  <NoteCard title="💡 나눗셈은 분수의 곱셈으로!">
    <div className="text-lg font-extrabold text-gray-700 leading-[1.7]">
      나눗셈은 <strong className="text-slide-red">분수의 곱셈</strong>으로 바꿔서 계산해!
    </div>
    <NoteExample>
      <MathInline tex={"A \\div B = A \\times \\dfrac{1}{B}"} />
    </NoteExample>
    {steps[10].quiz && (
      <QuizArea
        stepId={10}
        quiz={steps[10].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"A \\div B = A \\times \\dfrac{1}{\\phantom{B}}"} />
            <br />
            <span className="block text-center mt-2 text-lg font-extrabold text-gray-500">
              분모 = {blank}
            </span>
          </>
        )}
      />
    )}
  </NoteCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 11 — 나눗셈 분수 변환 법칙                    */}
{/* ──────────────────────────────────────────────── */}

<Step id={11}>
  <LawCard
    title="나눗셈의 분수 변환"
    formula={"A \\div \\color{#00AAEE}{B} = A \\times \\dfrac{1}{\\color{#00AAEE}{B}}"}
    example={
      <div className="space-y-3">
        <LawExample
          label="2개 나눗셈"
          tex={"A \\div B \\div C = A \\times \\frac{1}{B} \\times \\frac{1}{C} = \\frac{A}{BC}"}
        />
        <LawExample
          label="곱·나눗셈 혼합"
          tex={"A \\div B \\times C \\div D = \\frac{AC}{BD}"}
        />
      </div>
    }
    extraNote={
      <div className="mt-3 p-2.5 px-3.5 bg-slide-warning-bg rounded-[10px] text-center text-sm text-slide-brown font-bold">
        💡 ÷ 뒤의 것은 분모로, × 뒤의 것은 분자로!
      </div>
    }
  >
    {steps[11].quiz && (
      <QuizArea
        stepId={11}
        quiz={steps[11].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"A \\div B \\times C \\div D ="} /> {blank}
          </>
        )}
      />
    )}
  </LawCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 12 — (단항식) ÷ (단항식) 두 가지 방법         */}
{/* ──────────────────────────────────────────────── */}

<Step id={12}>
  <ConceptCard
    variant="default"
    formula={"12xy \\div 4x"}
    formulaSize="katex-law"
  >
    <div className="space-y-4 mt-2">
      <div className="bg-white rounded-[14px] border-l-4 border-slide-green py-3 px-4">
        <div className="text-sm font-extrabold text-slide-green mb-1.5">방법 1: 곱셈으로 바꾸기</div>
        <div className="katex-inline text-center">
          <MathInline tex={"12xy \\times \\frac{1}{4x} = \\frac{12xy}{4x} = 3y"} />
        </div>
      </div>
      <div className="bg-white rounded-[14px] border-l-4 border-slide-formula py-3 px-4">
        <div className="text-sm font-extrabold text-slide-formula mb-1.5">방법 2: 분수 꼴로 바꾸기</div>
        <div className="katex-inline text-center">
          <MathInline tex={"\\frac{12xy}{4x} = 3y"} />
        </div>
      </div>
      <div className="text-center text-sm font-bold text-slide-brown">
        어떤 방법이든 결과는 같아!
      </div>
    </div>
    {steps[12].quiz && (
      <QuizArea
        stepId={12}
        quiz={steps[12].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"12xy \\div 4x ="} /> {blank}
          </>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 13 — 약분 팁                                */}
{/* ──────────────────────────────────────────────── */}

<Step id={13}>
  <NoteCard title="💡 약분의 핵심">
    <div className="space-y-3">
      <div className="flex items-center gap-2.5 p-2.5 bg-emerald-50 rounded-[10px]">
        <span className="text-2xl">🔢</span>
        <span className="text-base font-extrabold" style={{color: '#149273'}}>수는 수끼리 약분!</span>
      </div>
      <div className="flex items-center gap-2.5 p-2.5 bg-pink-50 rounded-[10px]">
        <span className="text-2xl">🔤</span>
        <span className="text-base font-extrabold" style={{color: '#EE3B96'}}>문자는 문자끼리 약분!</span>
      </div>
    </div>
    <NoteExample>
      <MathInline tex={"\\frac{12xy}{4x} = \\frac{\\cancel{12}\\cancel{x}y}{\\cancel{4}\\cancel{x}} = 3y"} />
    </NoteExample>
  </NoteCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 14 — 연습 (1) 15a²b ÷ 5ab                  */}
{/* ──────────────────────────────────────────────── */}

<Step id={14}>
  <PracticeCard title="📝 나눗셈 연습 (1)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"15a^2b \\div 5ab"} />
    </div>
    {steps[14].quiz && (
      <QuizArea
        stepId={14}
        quiz={steps[14].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"15a^2b \\div 5ab ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 15 — 연습 (2) 12x⁴ ÷ 10x²y²               */}
{/* ──────────────────────────────────────────────── */}

<Step id={15}>
  <PracticeCard title="📝 나눗셈 연습 (2)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"12x^4 \\div 10x^2y^2"} />
    </div>
    {steps[15].quiz && (
      <QuizArea
        stepId={15}
        quiz={steps[15].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"12x^4 \\div 10x^2y^2 ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 16 — 연습 (3) 80xy⁴ ÷ 16xy²               */}
{/* ──────────────────────────────────────────────── */}

<Step id={16}>
  <PracticeCard title="📝 나눗셈 연습 (3)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"80xy^4 \\div 16xy^2"} />
    </div>
    {steps[16].quiz && (
      <QuizArea
        stepId={16}
        quiz={steps[16].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"80xy^4 \\div 16xy^2 ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 17 — 연습 (4) -4a⁴b³ ÷ (-2ab²)            */}
{/* ──────────────────────────────────────────────── */}

<Step id={17}>
  <PracticeCard title="📝 나눗셈 연습 (4)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"-4a^4b^3 \\div (-2ab^2)"} />
    </div>
    {steps[17].quiz && (
      <QuizArea
        stepId={17}
        quiz={steps[17].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"-4a^4b^3 \\div (-2ab^2) ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 18 — 혼합계산 개요                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={18}>
  <ConceptCard
    variant="white"
    formula={"(\\text{곱셈과 나눗셈의 혼합계산})"}
    formulaSize="katex-formula"
    description="거듭제곱이 있으면 지수법칙부터!"
  >
    <div className="mt-3 space-y-2.5">
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-red text-white text-sm font-bold">1</span>
        <span className="text-sm font-extrabold" style={{color: '#ED0690'}}>거듭제곱 → 지수법칙으로 계산</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-emerald-600 text-white text-sm font-bold">2</span>
        <span className="text-sm font-extrabold" style={{color: '#008670'}}>나눗셈 → 곱셈으로 바꾸기</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-formula text-white text-sm font-bold">3</span>
        <span className="text-sm font-extrabold" style={{color: '#0076C1'}}>약분이 되면 약분 먼저!</span>
      </div>
    </div>
    {steps[18].quiz && (
      <QuizArea
        stepId={18}
        quiz={steps[18].quiz}
        renderBlank={(blank) => (
          <>혼합계산에서 거듭제곱이 있으면 먼저 {blank}</>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 19 — 혼합계산 풀이                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={19}>
  <ConceptCard
    variant="default"
    formula={"(-2ab)^3 \\times 3a^2b \\div 4a^4b^2"}
    formulaSize="katex-formula"
  >
    <SolutionSteps>
      <SolutionLine label="거듭제곱 계산">
        <MathInline tex={"(-8a^3b^3) \\times 3a^2b \\div 4a^4b^2"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine label="나눗셈 → 곱셈으로">
        <MathInline tex={"(-8a^3b^3) \\times 3a^2b \\times \\frac{1}{4a^4b^2}"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine label="약분 후 계산">
        <MathInline tex={"= -6ab^2"} />
      </SolutionLine>
    </SolutionSteps>
    {steps[19].quiz && (
      <QuizArea
        stepId={19}
        quiz={steps[19].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"(-2ab)^3 \\times 3a^2b \\div 4a^4b^2 ="} /> {blank}
          </>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 20 — 혼합계산 연습                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={20}>
  <PracticeCard title="📝 혼합계산 연습">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"100a^5b^7 \\div (2ab^2)^2"} />
    </div>
    <div className="text-center text-sm text-slide-muted mb-3">
      힌트: <MathInline tex={"(2ab^2)^2 = 4a^2b^4"} />
    </div>
    {steps[20].quiz && (
      <QuizArea
        stepId={20}
        quiz={steps[20].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"100a^5b^7 \\div (2ab^2)^2 ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 21 — 완료 카드                              */}
{/* ──────────────────────────────────────────────── */}

<Step id={21}>
  <CompleteCard
    emoji="🎉"
    title="단항식의 곱셈과 나눗셈 학습 완료!"
  >
    <div className="text-left inline-block">
      단항식의 곱셈과 나눗셈을<br />모두 배웠어요!<br /><br />
      ✅ 곱셈의 교환법칙 · 결합법칙<br />
      ✅ <MathInline tex={"(\\text{단항식}) \\times (\\text{단항식})"} /><br />
      ✅ 나눗셈 → 분수의 곱셈 변환<br />
      ✅ <MathInline tex={"(\\text{단항식}) \\div (\\text{단항식})"} /><br />
      ✅ 곱셈 · 나눗셈 혼합계산<br /><br />
    </div>
    모든 퀴즈를 맞혔어요! 대단해요! 👏<br /><br />
    위로 스크롤해서 다시 복습할 수 있어요!
  </CompleteCard>
</Step>

</SlideContent>
