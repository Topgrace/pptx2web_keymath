import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { QuizArea } from '@/components/quiz'
import { IntegerOverviewStage } from '@/components/content/integer-overview-stage'
import { RationalOverviewStage } from '@/components/content/rational-overview-stage'
import { RationalInclusionSummaryStage } from '@/components/content/rational-inclusion-summary-stage'
import { ClassificationPracticeCarousel } from '@/components/content/classification-practice-carousel'
import { PositiveNegativeNumbersStage } from '@/components/content/positive-negative-numbers-stage'
import { SignedNumbersConceptStage } from '@/components/content/signed-numbers-concept-stage'
import { SignedNumberPracticeCarousel } from '@/components/content/signed-number-practice-carousel'
import { steps } from './steps-data'
import shape2Character from '@/assets/images/shape_2.png'

export const meta = {
  slug: '1-1-u9-integers-and-rational-numbers',
  title: '정수와 유리수',
  subtitle: '중1 수학 · 정수와 유리수',
}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg={shape2Character}
    speechText="이번 시간에는 0보다 작은 수를 생각해 보고 수의 세계를 확장해볼거야"
    topicTitle="09. 정수와 유리수"
  >
    <div className="mt-5 rounded-[18px] bg-white/90 px-5 py-4 text-left shadow-[0_6px_20px_rgba(0,0,0,0.06)]">
      <div className="mb-2 text-center text-[14px] font-extrabold tracking-[0.08em] text-[#2E7D57]">
        이번 시간에 배울 내용
      </div>
      <div className="space-y-2 text-[15px] font-bold leading-[1.8] text-slide-gray">
        <div>1. 정수의 뜻 이해하기</div>
        <div>2. 양의 정수, 0, 음의 정수 구분하기</div>
        <div>3. 유리수와 정수의 관계 살펴보기</div>
      </div>
    </div>

    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>양수의 반대말은 {blank}</>
        )}
      />
    )}
  </IntroCard>
</Step>

<Step id={1}>
  <IntegerOverviewStage stepId={1} />
</Step>

<Step id={2}>
  <RationalOverviewStage stepId={2} />
</Step>

<Step id={3}>
  <RationalInclusionSummaryStage stepId={3} />
</Step>

<Step id={4}>
  <PracticeCard title="분류 연습" subtitle="주어진 수들을 보고 알맞은 분류를 고르자">
    {steps[4].quiz && (
      <QuizArea
        stepId={4}
        quiz={steps[4].quiz}
        renderBlanks={(blank, solvedAnswers, meta) => (
          <ClassificationPracticeCarousel
            blank={blank}
            solvedAnswers={solvedAnswers}
            recentCorrectBlankId={meta.recentCorrectBlankId}
          />
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={5}>
  <PositiveNegativeNumbersStage />
</Step>

<Step id={6}>
  <SignedNumbersConceptStage />
</Step>

<Step id={7}>
  <PracticeCard title="부호로 나타내기" subtitle="밑줄 친 부분을 + 또는 -를 사용하여 나타내자">
    {steps[7].quiz && (
      <QuizArea
        stepId={7}
        quiz={steps[7].quiz}
        renderBlanks={(blank, solvedAnswers, meta) => (
          <SignedNumberPracticeCarousel
            blank={blank}
            solvedAnswers={solvedAnswers}
            recentCorrectBlankId={meta.recentCorrectBlankId}
          />
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={8}>
  <CompleteCard emoji="🎯" title="정수와 유리수 학습 완료!">
    정수와 유리수의 관계를 이해했고,
    <br />
    이제 수직선과 분류 도식으로 수의 범위를 구분할 수 있어요.
  </CompleteCard>
</Step>

</SlideContent>
