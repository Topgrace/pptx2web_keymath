import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { QuizArea } from '@/components/quiz'
import { NumberLinePointStage } from '@/components/content/number-line-point-stage'
import { NumberLinePointPracticeCarousel } from '@/components/content/number-line-point-practice-carousel'
import { NumberLinePointDragMissionCarousel } from '@/components/content/number-line-point-drag-mission-carousel'
import { NumberLineDistancePracticeCarousel } from '@/components/content/number-line-distance-practice-carousel'
import { NumberLineEquidistantPointCarousel } from '@/components/content/number-line-equidistant-point-carousel'
import { NumberLineFractionStage } from '@/components/content/number-line-fraction-stage'
import { steps } from './steps-data'
import shape2Character from '@/assets/images/shape_2.png'

export const meta = {
  slug: '1-1-u10-number-line',
  title: '수직선 위에 수 나타내기',
  subtitle: '중1 수학 · 정수와 유리수',
}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg={shape2Character}
    speechText="이번 시간에는 수직선 위에서 양수, 음수, 0의 위치를 살펴보고 수를 정확한 점으로 나타내는 방법을 배워볼거야"
    topicTitle="10. 수직선 위에 수 나타내기"
  >

    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>수직선에서 양수는 0의 {blank}에 있어요.</>
        )}
      />
    )}
  </IntroCard>
</Step>

<Step id={1}>
  <NumberLinePointStage quiz={steps[1].quiz} />
</Step>

<Step id={2}>
  <NumberLineFractionStage quiz={steps[2].quiz} />
</Step>

<Step id={3}>
  <PracticeCard title="수직선 위의 점 찾기" subtitle="수직선 위의 점에 대응하는 수를 고르자">
    {steps[3].quiz && (
      <QuizArea
        stepId={3}
        quiz={steps[3].quiz}
        completionContent={<NumberLinePointDragMissionCarousel />}
        renderBlanks={(blank, solvedAnswers, meta) => (
          <NumberLinePointPracticeCarousel
            blank={blank}
            solvedAnswers={solvedAnswers}
            recentCorrectBlankId={meta.recentCorrectBlankId}
          />
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={4}>
  <PracticeCard title="두 점 사이의 거리" subtitle="수직선 위의 두 점 사이의 거리를 구하자">
    {steps[4].quiz && (
      <QuizArea
        stepId={4}
        quiz={steps[4].quiz}
        renderBlanks={(blank, solvedAnswers, meta) => (
          <NumberLineDistancePracticeCarousel
            blank={blank}
            solvedAnswers={solvedAnswers}
            recentCorrectBlankId={meta.recentCorrectBlankId}
          />
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={5}>
  <PracticeCard title="같은 거리에 있는 점" subtitle="두 점으로부터 같은 거리에 있는 점을 찾자">
    <NumberLineEquidistantPointCarousel />
  </PracticeCard>
</Step>

<Step id={6}>
  <CompleteCard emoji="📍" title="수직선 위에 수 나타내기 완료!">
    수직선에서 양수와 음수의 위치,
    <br />
    점에 대응하는 수와 두 점 사이의 거리,
    <br />
    같은 거리에 있는 점까지 모두 정리했어요.
  </CompleteCard>
</Step>

</SlideContent>
