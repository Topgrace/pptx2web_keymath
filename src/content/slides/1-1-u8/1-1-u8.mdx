import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { StepCard } from '@/components/cards/step-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { SummaryCard } from '@/components/cards/summary-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { GcdLcmRelationSetupMotion } from '@/components/content/gcd-lcm-relation-setup-motion'
import { QuizArea } from '@/components/quiz'
import { MathInline } from '@/components/math'
import { steps } from './steps-data'
import shape2Character from '@/assets/images/shape_2.png'

export const meta = {
  slug: '1-1-u8-gcd-lcm-relation',
  title: '최대공약수와 최소공배수의 관계',
  subtitle: '중1 수학 · 소인수분해',
}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg={shape2Character}
    speechText="최대공약수와 최소공배수 사이에는 아주 중요한 관계가 있어! 두 수의 곱과 연결해서 한 번에 정리해 보자."
    topicTitle={"08. 최대공약수와\n최소공배수의 관계"}
  >
    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>결론부터 말하자면<br/>두 수의 곱은 최대공약수와 {blank}의 곱이다.</>
        )}
      />
    )}
  </IntroCard>
</Step>

<Step id={1}>
  <StepCard variant="white">
    <div className="mb-3 text-center text-[15px] font-bold leading-[1.7] text-slide-gray">
      최대공약수는 영어로{" "}<br />
      <span className="text-[#E25555] font-extrabold">G</span>reatest(가장 큰){" "}<br />
      <span className=" font-extrabold">C</span>ommon(공통의){" "}<br />
      <span className=" font-extrabold">D</span>ivisor(약수)로{" "}<br />
      첫글자 <span className="text-[#E25555] font-extrabold">G</span>를 따서 표시한다.
    </div>

    <div className="mb-4 text-center text-[15px] font-bold leading-[1.7] text-slide-gray">
      최소공배수는 영어로{" "}<br />
      <span className="text-[#E25555] font-extrabold">L</span>east(가장 작은){" "}<br />
      <span className="font-extrabold">C</span>ommon(공통의){" "}<br />
      <span className="font-extrabold">M</span>ultiple(배수)로{" "}<br />
      첫글자 <span className="text-[#E25555] font-extrabold">L</span>을 따서 표시한다.
    </div>

    {steps[1].quiz && (
      <QuizArea
        stepId={1}
        quiz={steps[1].quiz}
        renderBlanks={(blank, solvedAnswers) => (
          <div className="mt-4 space-y-4">
            <div className="rounded-xl border border-[#1F4F8A]/10 bg-[#F7FBFF] px-4 py-4">
              <div className="mb-3 text-center text-[14px] font-extrabold leading-[1.6] text-[#1F4F8A]">
                14와 21의 최대공약수는 7이고
              </div>

              <div className="mx-auto flex w-fit flex-col gap-y-2 text-center text-[31px] font-black leading-none text-[#2A2A2A]">
                <div className="relative grid grid-cols-[44px_18px_72px_72px] items-center pb-1">
                  <div className="flex justify-center">
                    <span className="flex h-9 w-9 items-center justify-center rounded-full bg-[#FFE65C] text-[24px] text-[#8A5A00]">
                      7
                    </span>
                  </div>
                  <div className="text-[28px]">)</div>
                  <div>14</div>
                  <div>21</div>
                  <div className="pointer-events-none absolute bottom-[10px] left-[48px] right-0 border-b-2 border-[#666666]" />
                </div>

                <div className="grid grid-cols-[44px_18px_72px_72px] items-center -mt-2">
                  <div />
                  <div />
                  <div className="flex justify-center">{blank('q14')}</div>
                  <div className="flex justify-center">{blank('q21')}</div>
                </div>
              </div>

              <div className="mt-3 text-center text-[15px] font-bold leading-[1.8] text-slide-gray">
                14와 21의 최소공배수는 {blank('lcm')}이다.
              </div>

            </div>

            {solvedAnswers.q14 && solvedAnswers.q21 && solvedAnswers.lcm && (
              <GcdLcmRelationSetupMotion
                renderBlank={blank}
                solvedAnswers={solvedAnswers}
              />
            )}
          </div>
        )}
      />
    )}
  </StepCard>
</Step>

<Step id={2}>
  <PracticeCard title="활용형 예시" subtitle="관계식을 이용해 모르는 값을 찾아 보자">
    {steps[2].quiz && (
      <QuizArea
        stepId={2}
        quiz={steps[2].quiz}
        renderBlanks={(blank) => (
          <div className="rounded-xl bg-[#F8F8F8] px-4 py-4 text-center text-[15px] font-bold leading-[1.9] text-slide-gray">
            두 자연수 <span className="font-extrabold text-[#E25555]">A</span>와 <span className="font-extrabold text-[#E25555]">70</span>의
            <br />
            최대공약수가 <span className="font-extrabold">14</span>이고
            <br />
            최소공배수가 <span className="font-extrabold">210</span>일 때
            <br />
            <span className="inline-flex flex-wrap items-center justify-center gap-1 align-middle text-[18px] font-extrabold text-[#2A2A2A]">
              <span>A×</span>
              {blank('num70')}
              <span>= 210×</span>
              {blank('gcd14')}
            </span>
            <br />
            <span className="inline-flex flex-wrap items-center justify-center gap-1 align-middle text-[18px] font-extrabold text-[#2A2A2A]">
              <span>따라서 A =</span>
              {blank('answer42')}
            </span>
          </div>
        )}
      />
    )}
  </PracticeCard>
</Step>

<Step id={3}>
  <SummaryCard
    title="최대공약수와 최소공배수의 관계 핵심 정리"
    layout="stack-below-tag"
    cases={[
      {
        tag: '최종 관계식',
        tagVariant: 'big',
        content: (
          <div className="py-2 text-center">
            <div className="mb-3 text-[14px] font-bold leading-[1.6] text-slide-gray">
              두 자연수 A,B의 최대공약수를 G, 최소공배수를 L이라 하면
            </div>
            <div className="whitespace-nowrap text-[24px] font-black leading-none text-[#1F3F68] sm:text-[32px]">
              <MathInline tex={'A\\times B = L\\times G'} />
            </div>
          </div>
        ),
      },
    ]}
  >
    {steps[3].quiz && (
      <QuizArea
        stepId={3}
        quiz={steps[3].quiz}
        renderBlank={(blank) => (
          <>두 수 14와 21의 최대공약수와 최소공배수의 곱은 {blank}이다.</>
        )}
      />
    )}
  </SummaryCard>
</Step>

<Step id={4}>
  <CompleteCard emoji="🧠" title="최대공약수와 최소공배수의 관계 완료!">
    이제 두 수의 곱을 보면
    <br />
    최대공약수와 최소공배수를 함께 연결해서 생각할 수 있어요.
    <br />
    <MathInline tex={'A \\times B = L \\times G'} /> 
  </CompleteCard>
</Step>

</SlideContent>
