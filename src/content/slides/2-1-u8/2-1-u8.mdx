{/* ── Imports ── */}
import { Step, SlideContent } from '@/components/mdx/step-wrapper'
import { IntroCard } from '@/components/cards/intro-card'
import { ConceptCard } from '@/components/cards/concept-card'
import { LawCard } from '@/components/cards/law-card'
import { NoteCard, NoteExample } from '@/components/cards/note-card'
import { PracticeCard } from '@/components/cards/practice-card'
import { CompleteCard } from '@/components/cards/complete-card'
import { QuizArea } from '@/components/quiz'
import { MathInline } from '@/components/math'
import { SolutionSteps, SolutionLine, SolutionArrow } from '@/components/content/solution-steps'
import { steps } from './steps-data'
import shape2Character from '@/assets/images/shape_2.png'

export const meta = {
  slug: 'unit8-fraction-calc',
  title: '분수 모양의 식 계산 (1)',
  subtitle: '중2 수학 · 단원2',
}

{/* ──────────────────────────────────────────────── */}
{/* Step 0 — 인트로                                  */}
{/* ──────────────────────────────────────────────── */}

<SlideContent>

<Step id={0}>
  <IntroCard
    characterImg={shape2Character}
    speechText="분수 모양의 식을 계산해보자~"
    topicTitle="분수 모양의 식 계산 (1)"
  >
    {steps[0].quiz && (
      <QuizArea
        stepId={0}
        quiz={steps[0].quiz}
        renderBlank={(blank) => (
          <>분모가 다른 분수를 계산하려면 먼저 {blank} 을 해야 해요</>
        )}
      />
    )}
  </IntroCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 1 — 분모가 같은 분수의 덧셈                   */}
{/* ──────────────────────────────────────────────── */}

<Step id={1}>
  <ConceptCard
    variant="white"
    formula={"\\frac{1}{3} + \\frac{2}{3} = \\frac{3}{3}"}
    formulaSize="katex-law"
    description="분모가 같은 분수의 덧셈은?"
  >
    <div className="mt-3 p-2.5 px-3.5 bg-emerald-50 rounded-[10px] text-center font-bold" style={{color: '#149273'}}>
      📝 분모는 그대로, 분자만 계산!
    </div>
    {steps[1].quiz && (
      <QuizArea
        stepId={1}
        quiz={steps[1].quiz}
        renderBlank={(blank) => (
          <>분모가 같은 분수의 덧셈은? {blank}</>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 2 — 분모가 다르면? 통분!                     */}
{/* ──────────────────────────────────────────────── */}

<Step id={2}>
  <ConceptCard
    variant="default"
    formula={"\\text{분모가 다르면?}"}
    formulaSize="katex-formula"
  >
    <div className="mt-3 text-center">
      <div className="text-5xl mb-3">🤔</div>
      <div className="text-lg font-extrabold text-slide-brown mb-2">
        분모가 통하게!
      </div>
      <div className="text-base font-bold text-slide-gray">
        분수 모양의 식도<br />
        분모가 같아야 덧셈, 뺄셈을 할 수 있어!
      </div>
    </div>
    <div className="mt-3 p-2.5 px-3.5 bg-slide-warning-bg rounded-[10px] text-center text-sm text-slide-brown font-bold">
      💡 분통해야지~ (분모를 통하게!)
    </div>
    {steps[2].quiz && (
      <QuizArea
        stepId={2}
        quiz={steps[2].quiz}
        renderBlank={(blank) => (
          <>분수의 덧셈·뺄셈은 분모가 {blank} 해야 해요</>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 3 — 분수 모양의 식 덧셈·뺄셈 원리             */}
{/* ──────────────────────────────────────────────── */}

<Step id={3}>
  <LawCard
    title="분수 모양의 식 공식"
    formula={"\\frac{A}{B} + \\frac{C}{B} = \\frac{A+C}{B}"}
    hint="💡 분모가 같으면 분자만 계산!"
    extraNote={
      <div className="space-y-2 mt-3">
        <div className="katex-inline text-center">
          <MathInline tex={"\\frac{A}{B} - \\frac{C}{B} = \\frac{A-C}{B}"} />
        </div>
      </div>
    }
  >
    {steps[3].quiz && (
      <QuizArea
        stepId={3}
        quiz={steps[3].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{A}{B} + \\frac{C}{B} = \\frac{\\phantom{A+C}}{B}"} />
            <br />
            <span className="block text-center mt-2 text-lg font-extrabold text-gray-500">
              분자 = {blank}
            </span>
          </>
        )}
      />
    )}
  </LawCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 4 — 통분하여 계산하기 예시                    */}
{/* ──────────────────────────────────────────────── */}

<Step id={4}>
  <ConceptCard
    variant="default"
    formula={"\\frac{x-y}{3} - \\frac{3x-y}{2}"}
    formulaSize="katex-law"
  >
    <SolutionSteps>
      <SolutionLine label="최소공배수 6으로 통분">
        <MathInline tex={"\\frac{2(x-y)}{6} - \\frac{3(3x-y)}{6}"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine label="분수를 하나로 합쳐서 쓰기">
        <MathInline tex={"\\frac{2(x-y)-3(3x-y)}{6}"} />
      </SolutionLine>
      <SolutionArrow />
      <SolutionLine label="분자 전개 및 정리">
        <MathInline tex={"\\frac{2x-2y-9x+3y}{6} = \\frac{-7x+y}{6}"} />
      </SolutionLine>
    </SolutionSteps>
    {steps[4].quiz && (
      <QuizArea
        stepId={4}
        quiz={steps[4].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{x-y}{3} - \\frac{3x-y}{2} ="} /> {blank}
          </>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 5 — 통분 과정 상세설명                       */}
{/* ──────────────────────────────────────────────── */}

<Step id={5}>
  <NoteCard title="📝 통분 과정 정리">
    <div className="space-y-3">
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-red text-white text-sm font-bold">1</span>
        <span className="text-sm font-extrabold" style={{color: '#ED0690'}}>분모의 최소공배수 구하기</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-emerald-600 text-white text-sm font-bold">2</span>
        <span className="text-sm font-extrabold" style={{color: '#008670'}}>각 분수의 분모·분자에 같은 수 곱하기</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-formula text-white text-sm font-bold">3</span>
        <span className="text-sm font-extrabold" style={{color: '#0076C1'}}>분자끼리 계산하고 정리하기</span>
      </div>
    </div>
  </NoteCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 6 — 통분 연습 (1)                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={6}>
  <PracticeCard title="📝 통분 연습 (1)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"\\frac{x-y}{3} \\text{ 의 분모를 6으로 바꾸면?}"} />
    </div>
    <div className="text-center text-sm text-slide-muted mb-3">
      힌트: 분모×2, 분자도 ×2
    </div>
    {steps[6].quiz && (
      <QuizArea
        stepId={6}
        quiz={steps[6].quiz}
        renderBlank={(blank) => (
          <>분자 = {blank}</>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 7 — 통분 연습 (2)                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={7}>
  <PracticeCard title="📝 통분 연습 (2)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"\\frac{a}{2} + \\frac{b}{5}"} />
    </div>
    <div className="text-center text-sm text-slide-muted mb-3">
      분모의 최소공배수: 10
    </div>
    {steps[7].quiz && (
      <QuizArea
        stepId={7}
        quiz={steps[7].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{a}{2} + \\frac{b}{5} ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 8 — 뺄셈 연습 (3)                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={8}>
  <PracticeCard title="📝 통분 뺄셈 (3)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"\\frac{a}{2} - \\frac{b}{7}"} />
    </div>
    <div className="text-center text-sm text-slide-muted mb-3">
      분모의 최소공배수: 14
    </div>
    {steps[8].quiz && (
      <QuizArea
        stepId={8}
        quiz={steps[8].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{a}{2} - \\frac{b}{7} ="} /> {blank}
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 9 — 통분 연습 (4)                           */}
{/* ──────────────────────────────────────────────── */}

<Step id={9}>
  <PracticeCard title="📝 통분 연습 (4)">
    <div className="text-center text-lg font-extrabold text-slide-gray mb-2">
      <MathInline tex={"(3x-5) \\text{를 분모 3으로 바꾸면?}"} />
    </div>
    <div className="text-center text-sm text-slide-muted mb-3">
      힌트: 정수는 분모가 1인 분수!
    </div>
    {steps[9].quiz && (
      <QuizArea
        stepId={9}
        quiz={steps[9].quiz}
        renderBlank={(blank) => (
          <>
            <MathInline tex={"\\frac{3x-5}{1} = \\frac{\\phantom{3(3x-5)}}{3}"} />
            <br />
            <span className="block text-center mt-2 text-lg font-extrabold text-gray-500">
              분자 = {blank}
            </span>
          </>
        )}
      />
    )}
  </PracticeCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 10 — 정리                                   */}
{/* ──────────────────────────────────────────────── */}

<Step id={10}>
  <ConceptCard
    variant="white"
    formula={"\\text{분수 모양의 식 계산 정리}"}
    formulaSize="katex-formula"
  >
    <div className="mt-3 space-y-2.5">
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-formula text-white text-sm font-bold">✓</span>
        <span className="text-sm font-extrabold text-slide-brown">분모의 최소공배수로 통분</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-formula text-white text-sm font-bold">✓</span>
        <span className="text-sm font-extrabold text-slide-brown">분자끼리 계산, 분모는 그대로</span>
      </div>
      <div className="flex items-center gap-2 justify-center">
        <span className="inline-flex items-center justify-center w-7 h-7 rounded-full bg-slide-formula text-white text-sm font-bold">✓</span>
        <span className="text-sm font-extrabold text-slide-brown">결과 정리 (동류항끼리 모으기)</span>
      </div>
    </div>
    {steps[10].quiz && (
      <QuizArea
        stepId={10}
        quiz={steps[10].quiz}
        renderBlank={(blank) => (
          <>통분할 때 분모는 {blank} 로 맞추면 좋아요</>
        )}
      />
    )}
  </ConceptCard>
</Step>

{/* ──────────────────────────────────────────────── */}
{/* Step 11 — 완료 카드                              */}
{/* ──────────────────────────────────────────────── */}

<Step id={11}>
  <CompleteCard
    emoji="🎉"
    title="분수 모양의 식 계산 (1) 학습 완료!"
  >
    <div className="text-left inline-block">
      분수 모양의 식 계산을<br />모두 배웠어요!<br /><br />
      ✅ 분모가 같은 분수의 덧셈·뺄셈<br />
      ✅ 통분의 원리 (최소공배수)<br />
      ✅ <MathInline tex={"\\frac{A}{B} \\pm \\frac{C}{B} = \\frac{A \\pm C}{B}"} /><br />
      ✅ 분모가 다른 분수 모양의 식 계산<br /><br />
    </div>
    모든 퀴즈를 맞혔어요! 대단해요! 👏<br /><br />
    위로 스크롤해서 다시 복습할 수 있어요!
  </CompleteCard>
</Step>

</SlideContent>
